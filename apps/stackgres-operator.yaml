apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: stackgres-operator
  namespace: argocd
spec:
  destination:
    namespace: stackgres
    server: https://kubernetes.default.svc
  project: default
  source:
    repoURL: 'https://gitlab.com/ongresinc/stackgres/install/helm/stackgres-operator/'
    targetRevision: release-1.4.0
    path: .
    helm:
      values: |
        adminui:
          service:
            type: ClusterIP
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true